# Gradle
.gradle/
build/
*.apk
*.aab

# Local properties
local.properties

# WebAssembly (large binaries)
app/src/main/assets/pandoc.wasm
*.cwasm
tools/pandoc.wasm

# IDE
.idea/
*.iml
*.iws
*.ipr
.DS_Store

# Build outputs
app/build/
app/.cxx/

# Kotlin compiler errors (temp files)
.kotlin/errors/

# Tool config dirs (not project files)
claude-code/
opencode/

# Real FFmpeg binary (built via tools/build-ffmpeg.sh, ~40-50MB)
# The 4KB stub IS tracked in git; the real binary must be built locally.
# After first successful build, uncomment the line below to exclude it:
# app/src/main/jniLibs/arm64-v8a/libffmpeg.so
